#include "config.h" // Include the configuration constants
#include "init.h"   // Include the initialization function

int main() {
    init();
    // Begin your B+ tree operations
    return 0;
}
